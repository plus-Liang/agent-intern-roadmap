# 变量名与仓库根目录的 .env.example 保持一致：对话模型统一走智谱，embedding 走本地 fastembed（无需 Key）。
ZHIPU_API_KEY = "your_api_key_here"
# ZHIPU_BASE_URL = "https://open.bigmodel.cn/api/paas/v4"
# ZHIPU_CHAT_MODEL = "glm-5.3-flash"
USE_RERANKER = "false"
APP_DB_PATH = "/tmp/applications.db"
