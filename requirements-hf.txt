# Slim deps for Hugging Face Spaces (FastAPI + Chainlit service).
# Versions kept in sync with requirements-full.txt; no streamlit / playwright / torch /
# sentence-transformers / scikit-learn (local-only heavy deps).
# NOTE: numpy==2.5.3 requires Python >=3.12, so use 3.12+ here and in CI.
fastapi==0.141.1
uvicorn==0.52.4
chainlit==2.12.0
APScheduler==3.11.3
openai==3.8.0
chromadb==1.5.9
rank-bm25==0.2.2
jieba==0.42.1
python-dotenv==1.2.3
json5==0.15.0
pypdf==6.18.1
pandas==3.0.5
numpy==2.5.3

# Imported directly at runtime, pinned instead of relying on transitive deps:
# httpx (LLM calls), requests (niuke/ncss JSON APIs), pydantic (FastAPI models),
# PyYAML (config/scraping.yaml), fastembed (local embeddings for RAG).
httpx==0.28.1
requests==2.34.2
pydantic==2.13.5
PyYAML==6.0.3
fastembed==0.8.0
